[package]
name = "arrow-kernels-activations"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
arrow-kernels-common = { path = "../arrow-kernels-common" }
arrow = { workspace = true }
num-traits = { workspace = true }
